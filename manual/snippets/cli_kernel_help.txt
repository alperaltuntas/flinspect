usage: groundline kernel [-h] SUBCOMMAND ...

positional arguments:
  SUBCOMMAND
    list      kernels in the manifest + basic status
    show      print one kernel's generated Lean defs
    generate  (re)write the generated Lean modules
    verify    regenerate, byte-diff against the committed files, lake build
              (non-zero exit on drift or failure)

options:
  -h, --help  show this help message and exit
