/-- Generated from `scale_clip_acc_loop` in `toy_kernel_loop.f90` (flang with-sema dump).
Outputs `(b)` — the `intent(inout)` arguments, modeled functionally over ℝ. -/
def scale_clip_acc_loop (a b s lo : ℝ) : ℝ :=
  let w := s * a
  if w < lo then
    b + lo
  else b + w
